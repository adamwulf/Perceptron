I have an app I want you to build. This a model of how perceptrons work. 
We are in the planning stage, and I want you to get the instructions. 

First: Notice that the basic layout of screen parts is seen here: c:\temp\perceptrons.png. 

This program has a one-to-one relation of switches to LEDs (the LEDs show switch state) to knobs/dials (the dials
can select from range from -30 to 30), and then there is one extra knob/dial on the bottom that is a bias (not associated with any LEDs).
The switches, the LEDs and the knbos are all shows in squre grid, each switch connects to one LED and that to one dial/knob. All of this is
connected to an processor board that outputs to a analogy gague that goes from -100 to 100. I want you to implement a visual
form of this, with switches, the LED outputs (showing switch state), the connected knbos/dials, and then the final output taking all the knobs as
inputs and outpting a single microapere. Follow the instructions in the video if you are able. If you are not able, ask me and I will try to
capture the notes somehow. Don't do any work yet, just gather up all this information, and then if you can do it, we can plan our steps.

This is a perceptron. The machine shocked the world of the 1950s by learning to recognize patterns completely
automatically. And today the algorithm it implements has become the core building block of AI systems like chat GPT. But why is this the
atomic unit of the intelligent systems we have today? 

The perceptron works by processing patterns we input using these switches, like this T shape. Switches in the up position output a
positive voltage, and switches in the down position output a negative voltage. Each switch is connected to an indicator LED, and then to one
of these dials.

Following are the rules for the program, I took these from a transcription of https://www.youtube.com/watch?v=l-9ALe3U-Fg. Do NOT load that
youtube video, just use the instructions I have for you here: 
"Rotating a dial multiplies the output of the switch by the number shown on the dial. And the meter shows the result of adding the signals from
all the dials together. Now, is there a way to configure our dials, such that the perceptron always outputs a positive signal for
T shapes, without putting a negative value for other types of shapes, like this J shape. Note that our shapes won't be in the same position
each time. Remarkably, it turns out that if a configuration of dials that solves our problem exists, there is a simple procedure we can
follow that is guaranteed to find it every time. Starting with this T shape, our meter is showing a value close to zero, but we want it to be
positive. In this case, our procedure tells us to turn all the knobs that are switched on to the right by a constant value called the learning 
rate, and to turn all the knobs that are switched off to the left by the learning rate. Our next pattern is a J shape, so we want our machine to 
output a negative value. However, the current dial configuration outputs a positive value. In this case, our procedure tells us to turn down all 
the dials that are switched on and turn up all the dials that are switched off. Moving to our next pattern, a shifted J shape. Our perceptron 
again outputs a positive value instead of the desired negative value. So we again turn down all the switches that are on and turn up all the 
switches that are off. Arriving at our fourth and final example, the current configuration of dials outputs the correct positive value that we 
expect for a T. In this case, our procedure tells us to leave our dials alone. Recycling back through our patterns, we see that our machine has 
learned to correctly classify all four examples. This procedure was discovered in 1957 by the psychologist Frank Rosenblad, and is known as the 
perceptron learning rule."

I want the grid to be configurable (4x4 but user can go larger)
Make the Grid size control be a dial, and have it go from 1 to 10. Start it off at 4, and make 1 be poiting straight up.
And make the learning rate be adial with values from 0.1 to 5.0 with 0.1 steps. And put the word "Grid" in a metal plate
like you have "Bias" and "Learn Rate" also in a metal plate.

I want automated and manual mode "Mode: Manual" button (the user can select, make it bottom middel of the screen). Beside it, a button called 
"Learn +" and "Learn -", put in the style of metal plates with four corner divits.

In automated mode, when you user inputs something on the switches, they can press "Learn +" that will automatically adjust all the dials that
have selected swtiches. "Learn -" will decrese the voltage by the "Training Amount/Rate" 
Make the learning rate dial go form -30 to 30 with a step rate of .05, and with Zero pointing up.

Or they can do it manually. 

I also want a green LED beside the output meter. On means + voltage, Off means - voltage. 

I want this to be a stand-alone Winform app in C#. 

I don't want any borders or frame on the app. I want the bacground all black. I want there to be a red X in the top right
corner to close the app. I want the app to use fixed Consoloa font throughout (to make it look 19980s text type). I want the name of the app
"Mark 1 Perceptron Simulator by Frank Rosenblatt, Cornell, 1958" in the top center of where the main windows top bar would go 
(but, again, I don't want you to use any of the ugly windows form default look). 

If the user clicks on a dial, it is the active dial. Then if they press the up or down arrow, it will change its value by whatever 
the step size is of that dial/knob. None of my screen elements should have any tabstops. That way when I use arrows to change the 
values of the gauges, they are not interpreted as tab stops. 
For ALL dials/knbos The up and down arrorws, and the left and right arrows, should not tab or move to the next control. The up and down left and right
 rrows control the knob. I don't care what you have to do to make this true, make it true. Up and right, increase the value, down and left decrease it. The
  tab key can take the user to the next item, but the arrow keys MUST be captures and the control must use them as knob controls.

You have seen the png I gave you, so you know the basic layout. You can put the switches and the LEDs together, instead of
in different grids. You can show the LED and the switch very close so the association is obvious. The panel of knobs can be off to the right of
that pannel. The panel of knobs has one extra knob which is a bias knob. The outputs from all the knobs don't need to be shown leading into a
breadboard like in the perceptrons.png I gave you. I gave you that image as a guide.

Make the program sizeable when the grid changes size. 

Put hoover over texts explaining each button. Add a master switch to turn off all the switches with LEDs. And for the knobs, orient them so that pointing straight
UP is 0 and is the default. And take the word off the gage you are showing (being amperes is not important). When the user
makes the grid larger than 4x4, auto-grow the screen, and make it so that everything fits without overlap.  I need to be able to grabe the 
edge of the window, and size it. The sizing of the main window needs enabled. Show a very small red set of diagnoal lines in the bottom right 
corner indicating that the user can grave the bottom right corner and size the window. The "Bias" label should exist just above the Bias knob, 
so that the number the knob is turned to, does not overlap. The green output light should only be on if the output is greater than zero.
Put a label that explains how to set the knobs, something like, "1.Set your switches. 2. If the led is red, turn the dial by X. 3. If the led
is off, turn the dial by -X. 4. Repeat" --  make the label look like one of those metal plates from a 1950s machines.

Make the header title to "Mark I Perceptron Simulator by Frank Rosenblatt, Cornell, 1958". Replace the Reset button with a master switch that is on if
ANY of the switches are on, and off, if all the switches are off. And when the Master Switch is On, turning it off will turn off all the LEDs. And if it
is Off, it will turn on all the LEDs. Place the Master Switch positioned below all the other switches, similarly loctated as the bias dial is to the
other dials. Give it a label in a steally looking metal label that says, "RESET". Also make the Label for "BIAS" have a similar metal plate.
  
Remove the Reset button from the page. It won't be needed. Also, the metal plate that shows the instructions, the text needs to be easier to read, so
make the text brigher.

For the Bias label. I want it to look like a metal plate. You can put it to the right of the Dial to give it room. The isntructions are on a "Metal
Plate" (my defintion of a metal plate is what holds the instructions). Also, put the "Reset" onto a metal plate approriately sized. And put a hoover over
text for the Grid size and the Rate input so the user can know what these are.  The metal plates that hold the word "Bias" and "Reset" need to have divits 
on the four corners as to where the plates are attached to the machine/background. And in both cases, the plates should be on the left side of the control 
to which they relate.

And make all the buttons, "Mode: Manual" and "Learn +" and "Learn -" be metal plates like everything else
(just clickable like the operator's manual). Move these buttons "Mode: Manual" and "Learn +" and "Learn -" to the center of middle of teh screen 

Step size for when we press left, right, up down on a dial needs to be configurable. The main dials part of the perceptron engine, have step size of 0.05 (a
very fine tune control). And in a sudued small metal plate above the switches, put a tag that says "Off = -1v, On = +1v"

----------------------------------
# Next feature is a manual
  
In the bottom right of the main pate, we need a manual/tutorial.  To access the manaul, the user presses the metal label that says, "Operator's Manual" 
(again, for this metal label, put four divits on the four corners like the Bias plate). And if the user presses the plate, then you show a 
dialog styled like our main page. Put the operator's manual button down in the bottom right, it will launch the dialog for reading pages (as follows).

This dialog will hold a page. One page. The first page will have a table of contents like an old manual (use white paper with black font, and the font
has to look like one of those old manuals that has been decalssified by the CIA (you may have to create fonts, find a font, etc). Make the background
look like a photo copied paper. And put a watermark "Declassified" diaganal and subdued across all the manual pages.
And on the "declassification" watermark, add smaller subtest centerend beneatht the DECLASSIFIED put "Declassified 
in 2005 as per the 1995 Excutive Order 12958 3.4"
  
And the first page will have the table of contents. Multiple chapters are in this book/manual. They are:

Introduction. Operating Procedures. Credit/About. Build It Yourself. 

The table of contents will list these. To make it look cool, put fake chapters that have been redacted out. For each chapter do the chapter name
and ........ Page Number (make up fake page numbers). Make sure the dots are not running past the page numbers 
For example, this would be wrong:  "3..", "7...", "18...". just fill the gap completely between title and page number

Each of these chapters is clickable (excpet the redacted ones), when clicked, bring up a page onto the dialog that 
replaces the current view. Also, for any page selected, there will be at home, a "TOC" link at the top, to take the user back to the 
table of contents. Add a Next Page for pages that would scroll off the bottom of the page. At the bottom center of the page dialog, 
add <-Prev and Next-> pages, and show on the page the same fake number you got from the fake numbers in the talbe of contents.
Use relative/dynamic positioning based on actual parent dimensions, not hardcoded pixel values. Add scroll handling for variable-length content.
Don't magic numbers like PAPER_WIDTH = 660, instead query the real dimensions when placing controls.
The manual dialog must be wide enough to show the fake page numbers on the table of contents. 

Here are the chapters, I describe each starting with a #

# Introduction Chapter Definition:
If the user presses the Introduction chapter, it shows them this:  

"In 1958, Frank Rosenblatt, a research psychologist at the Cornell Aeronautical Laboratory, introduced the Perceptron, the world's first artificial neural network capable of learning from experience. 

Key Developments in 1958. 
The Algorithm: Rosenblatt published his seminal paper, "The Perceptron: A Probabilistic Model for Information Storage and Organization in the Brain," in Psychological Review. 
It defined the first machine learning algorithm for artificial neurons, using a weighted sum of inputs to make binary classification decisions.

Initial Demonstration: Using a 5-ton IBM 704 computer, Rosenblatt demonstrated the perceptron's ability to "teach itself" to distinguish between cards marked on the left
versus the right after just 50 trials.

Public Impact: The U.S. Office of Naval Research unveiled the invention in July 1958, leading to sensational New York Times headlines that predicted the "embryo of an electronic computer" 
would soon walk, talk, and be conscious. 

The Perceptron Machine (Mark I). 
While the 1958 demonstrations used software simulations, Rosenblatt later realized the concept in physical hardware

Physical Build: The Mark I Perceptron was completed in 1960 at Cornell. Components: It featured a "camera eye" with 400 photocells (a 20x20 pixel array) and used electric motors to adjust potentiometers, which acted as the machine's "variable weights".

Capabilities: It was primarily designed for image recognition, successfully learning to identify basic geometric shapes like triangles and squares."

--------------
# Operating Procedures Chapter Definition
If the user presses the Operating Procedures, give them instructions on how to do the T and J example from the dialog above

--------------
# Credits/About:
In this chapter, give credit to "Welch Labs"
Put a link to the youtube video (https://www.youtube.com/watch?v=l-9ALe3U-Fg)
Put a link to the original manaul: https://apps.dtic.mil/sti/tr/pdf/AD0236965.pdf
Created by S. Rives with full use of Claude Code AI: https://github.com/srives 

--------------
#Build it Yourself
Tell the user the following:

"This program was build 100% with AI through prompts as an hommage to the pioneers of Nerual Networks.
You can take the exact same prompts I used, feed it into Claude Code, and have it build a replica of this program.
Copy the accompanying perceptrons.png to c:\temp\, then feed the text from prompt.txt into Claude Code to recreate
this program. I predict, one day, we won't share code, we will share prompts. And so I am starting the trend today."

Give the user a button to see the perceptrons.png file (which is in resources\) 
Give the user a notpead button to pull up the resources\prompt.txt 
Both of these files need to lives in the same path as where this program runs so that the program can find them.
And for the buttons on this chapter, put the buttons closer to the bottom of the dialog.

Note to Claude: I want you to "suck up" and embed the files that are in the resources directory so they are build right into the program as virtual 
files, so that when the user needs to access the prompt.txt, it comes out of the program. At build time, you create the EXE and you consume these two resoruces 
into the build, and then when the user presses "View Rererence Image", you produce for them the image served up not from your own memory as an EXE.

-----------------------
That's the book feature all done.

I want a descriptive metal plate across the top, just below the title bar area (snugging it real close), centered. I want it to have the math formulat for the
perceptron. Show that it is taking the values and summing them up. Of course, make it accurate what is happening. So that the value of the final output 
is understood by the user as to whereforth it came. Use smaller fonts as needed, and use only one line of text. and make the metal plate look like the other 
metal plates, and do NOT make it span the whole width of the screen, just have it old the text.

Add a save and a load option, where the user can save all the values they have dialed in, or they can load all the values preiviusly saved. You will adjust
the grid size based on what the user has saved. So you may have to shrink or grow the grid as needed. And, as always, make these metal plate looking
buttons, and put them in the bottom to the left of the operator's manual button.

To be clear, when I press the "Learn +", you will go through and adjust all the dials by the
learning amount, positive for the lights on, and negative for the lights off. And if I press "Learn -", you will go through and adjust all the dials by the
learning amount, negative for the lights on, and postive for the lights off. If this is correct, I would like you to update our operators manual. Add a new
chapter called, "The Alogorithm" as the chapter before the about chapter, and put this information in it. I would also like you to clarify the math (same as
what you put on the top label on the top of the main screen, only now you can say a few more words. You will need to update the table of contents, and
generate new fake page numbers to make all this work.  

Okay, we need a debug window that runs analog to and beside the main window. If the user pressed [Debug] (another bottom button, with a metal plate design),
then this modaless dialog pops up, that aysnchronisly knows about all the switches and all the dials. But it doesn't draw switches and dials. It draws
instead a vertical line on the left side of the dialog, a vertial set of cricle nodes--each one standing for the different input switches in the main menu.
And there is a 1 or a 0 feeding in from the left of these circles/nodes, rpresenting the state of the switches (1s and 0s for the switch states, and
dynmically updating if a switch changes). Then, we have horizontal lines leaving that node, going to the right and meeting the middle layer node that
corresponds to it (these being the dials). And these dials are represented as little circles, and they have outputs, showing the value of the output over
the top of the line as it leaves the node. All of the middle layer lines coming from the midle layer, meet the output single output node. And, coming out of
that output node / circle, is a line going to the final output value, showing the final number. This is very much like a three layer nerual net, only we
have just one input and one output nodes. We also have the bias node feeding into the output. This dialog should scale and make the circles smaller as
needed. The coloring scheme of this dialog is black background, with red input nodes being nodes receving a 1 (a positive input), and white otherwise. All
lines are either white (for no value coming out), or light blue if a value is coming out. And the numbers should be rendered with postive numers being
green, and negative numbers being yellow. This dialog will be sizable, and if the user sizes it, the redraw can account for space to make the circle nodes
bigger. IF the circles are big enough, put the value of the dial in the cricle itself showing what it would output if a value was coming in to excite it.

Using Visual Studio Publishing methods, make this program have a publish profile so that I can build a single EXE and release it. Have it publish to 
c:\perceptron_release\

We have solved the arrow problem on the grid controls. For this reason, you can renable the tab control, and let tab move to the next control left to right, 
top to bottom. The space bar should enable or disable a switch.


Put this information "The bias allows the decision boundary to shift away from the origin, which is essential for problems like AND/OR gates where the
threshold isn't at zero" under the section on "The Algorithm". 

Then add two new buttons: Add a Save button that looks like a push button with the word
"SAVE" above it in metal plate lable. And Make another button like it, with a "LOAD" metal plate above it. I want the buttons to look like old buttons
from likr from the 1950s. Small buttons, mechanical looking, where by the Save, the user can save all the values they have dialed in, or by load, they
can load all the values preiviusly saved. You will adjust the grid size based on what the user has saved. So you may have to shrink or grow the grid as
needed. And, as always, make these metal plate looking buttons, and put them in the bottom to the left of the operator's manual button.
Change the save and Load buttons by adding faint light much like the learn button (redish). Make the "Manual" button yellow like the brain button.
To scale the grid up and own, the Switch Control must be able to scale based on actual Width/Height.

I want four triangular arrow buttons that cluster together (sort of like the four arrows on the TI-83). I want this collection of buttons to be of the
same style as the Save and Load buttons, just triangular. I want each one to have very pale lighting effects of yellow light. And I want this cluster of
four arrow buttons like in c:\temp\arrows.png in arrangment, but color and style like what we have already designed for Save and Load.
Put these arrows beside the reset switch (to the right of it). These buttons, each one, moves the switch states left right up or down.
So that if you have a T of switches set, and press the Right button, the T will shift right.


I want you to move the "Grid Size" plate and the Grid Size Dial to the left so that they are as far from the left edge of the window as the leftmost switches above are.
  Then I want you to create a new dial on our UI, to the right of those, with a metal plate that says, "Math", and I want it be above a dial. That dial will have values printed
  outside the hash marks going around the dial. At the 1 o'clock position of the dial will be the value 1958 (have the hash mark have 1958 beside it). The dial will default to this
  value/position, and it means that the math formula we are using is the 1958/1959-formula (Rosenblatt's Perceptron) -- which you already have coded. Then I want the 3 o'clock
  position to have a hash mark of 1960, and this is going to correspond to the Widrow-Hoff Rule (aka, aka Least Means Squre/LMS, which I will define shortly for you to know about.
  And the 7 o'clock position will have a hash mark of 1986 and will correspond to the current Multi-Layer network rule you have coded. So, What I am asking is that we use the
  network configuration as defined on the main screen, but as for the activation function (i.e., the formulat we use on a middle layer node where it takes its inputs, does some
  math, and outputs a signal) will do different math based on the "Math" function selected by the Math Dial. Tell me if this makes sense before you change the code. Tell me what you
   think you are going to do, and I will judge your accuracy. Now, as to the 1960 formulat (the Widro-Hoff rule), here it is: In 1960, Bernard Widrow and Ted Hoff developed a
  learning rule for their ADALINE (Adaptive Linear Neuron) system that differed mathematically from Frank Rosenblatt's original perceptron. The "math" used by Widrow and Hoff is
  formally known as the Widrow-Hoff Rule, the Delta Rule, or the Least Mean Squares (LMS) algorithm. 1. The Core Update Formula The rule updates the network's weights (\(w\)) based
  on the difference between the desired target (\(d\)) and the raw linear output (\(y\)) before any activation function is applied:\(\mathbf{w}(t+1)=\mathbf{w}(t)+\eta
  (d(t)-y(t))\mathbf{x}(t)\)\(d(t)\): Desired output (target).\(y(t)\): Actual linear output (the dot product \(\mathbf{w}^{T}\mathbf{x}\)).\(\eta \): Learning rate (a small
  positive constant).\(\mathbf{x}(t)\): Input vector. 2. Key Mathematical Differences While the original perceptron rule only updated weights when a classification error occurred,
  the Widrow-Hoff rule focuses on minimizing a continuous Loss Function: Loss Function: It uses the Mean Squared Error (MSE): \(L=(d-y)^{2}\). The goal is to minimize this squared
  difference using gradient descent.Pre-Activation Feedback: Widrow-Hoff uses the error from the linear combiner (the sum \(\sum w_{i}x_{i}\)) to adjust weights, whereas
  Rosenblatt’s perceptron used the error from the final quantized/binary output (+1 or -1).
  
I hit enter accidentally. Let me resay it. I realize we don't need the multi-layer switch now that we are creating this new Math Dial. What we need is that the new dial has more
  values than I first specified last. Let me redefine it: We still keep the hash mark "1958" at 1'oclock position, and it represents the perceptron network as originally done in
  this software (each input swith connects to one middle layer dial/node, and all the middle layer dials/nodes connect to the one output node/guage. But now, at the 2'oclock
  postition, there will be a hash-mark that says "1958+" (to be defined below).Then at 3 o'clock, the hash mark will be "1958m" (to be defined below). At 4oclock is the hash mark,
  "1958/+" (see below), and 5o'clock hash is "1958/m" (see below). Next we leave the 6 and 7 and 8 o'clock hash marks with nothing (they are not hooked up to anything). Next, we go
  to the 9 o'clock, which is now our old "1960" hash mark--also being the same 1960, Widrow-Hoff Rule (aka, Least Means Squre/LMS) rule we already defined above. Finally, at 10
  o'cloc is our the "1986" rule which is already implemented (the multi-layer network which we already have implmented). Now let me define the new functions/behaviors for the
  2,3,4,5 oclock setting. What happens when the 2'clock ("1958+") is selected, is as follows: This is sorta like the 1958 perceptron Mark 1, except now I want you to make it so that
   all the inputs each connect to all the outputs, just like in the mult-layer network. HOWEVER, this is not the modern multilayer math, instead you will be using math as follows,
  and with no back prop logic: Namley, this will use the Mark 1 Prceptron formula and alogrithm for hidden layer nodes, it will just handle multiple inputs on each of the hidden
  nodes/dials, and the logic for each middle layer node is that each node loops through all its inputs, and sums all the values for the formula: input x dial value + bias, and
  output that sum. This is code-named the 1958_SUM_RULE, or "1958+" (make the code have this code name in the comments with the corresponding function so I can reference it). The
  Math Dial for 3'oclock is "1958m", and it is the same as the 1958+ except it averages all of its sums, and outputs the avergage (m stands for mean). This dial's alogrithm will be
  called the "1958_AVG_RULE" in the code comments" or also "1958m". Next is the 4'ocloc dial position labled "1958/+" and what it does is first divide each input value by the number
   of switches in the input array (if there are 16 total switch inputs, then the input value into a middle layer node is divided by 16). What happens then, is each middle layer node
   takes all of its inputs, and does the formula for each input: input/num_switches * dial_weight + bias. It adds up all those values and outputs it as its result. This code named
  the "1958_DIV_SUM_RULE" or "1958/+". Finally, the 5'oclock will be "1958/m" which will be like the "1958/+" in that it divides all of its input values by the number of
  switches/input nodes, and does the same forumla and sums all the values, only for its output, it sendd the average (hence the "m"). this is called the "1958_DIV_AVG_RULE" or
  "1958/m". Tell me if all of this makes sense, and if you can do it. Also tell me if this logic has been documented before. What I want, basically, is the ability to configure the
  math in the middle layer nodes, and the ability to configure if all the inputs connect to all the middle layers, or if only one input connects to one middle layer node. I believe
  I have achieved this definition here, I just need to know if you understand it and if you can implement it cleanly. Make the code eleogant, and refactor it if needed. But the end
  of the day, I may add more values to the math dial (more rules), so be ready for future expansion.